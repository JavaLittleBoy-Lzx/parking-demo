# 巡检员值班状态管理功能实施方案

## 📋 功能概述

为巡检员提供值班/离岗状态管理功能，离岗时不接收违规、超时等微信消息推送。

### 核心功能
1. ✅ 导航栏一键切换值班状态
2. ✅ TabBar徽标实时显示状态
3. ✅ 状态横条醒目提示
4. ✅ 小程序内提醒上岗
5. ✅ 推送消息自动过滤

---

## 🎯 实施步骤

### Phase 1: 数据库改造（10分钟）

#### 1.1 patrol表添加字段
```sql
-- 添加消息通知开关字段
ALTER TABLE patrol 
ADD COLUMN notification_enabled TINYINT(1) DEFAULT 1 
COMMENT '消息通知开关：1=接收（值班中），0=不接收（离岗）';

-- 添加最后状态变更时间
ALTER TABLE patrol 
ADD COLUMN last_status_change_time DATETIME 
COMMENT '最后一次值班状态变更时间';

-- 添加索引（用于查询值班中的巡检员）
CREATE INDEX idx_notification_enabled ON patrol(notification_enabled);
CREATE INDEX idx_community_notification ON patrol(community, notification_enabled);
```

#### 1.2 值班状态变更日志表（可选）
```sql
CREATE TABLE patrol_duty_log (
  id INT AUTO_INCREMENT PRIMARY KEY,
  patrol_id INT NOT NULL COMMENT '巡检员ID',
  patrol_name VARCHAR(50) COMMENT '巡检员姓名',
  openid VARCHAR(100) COMMENT '微信openid',
  community VARCHAR(100) COMMENT '所属小区',
  old_status TINYINT(1) COMMENT '变更前状态：0=离岗，1=值班',
  new_status TINYINT(1) COMMENT '变更后状态：0=离岗，1=值班',
  change_time DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '变更时间',
  change_source VARCHAR(50) DEFAULT 'APP' COMMENT '变更来源：APP/ADMIN/SYSTEM',
  INDEX idx_patrol_id (patrol_id),
  INDEX idx_change_time (change_time)
) COMMENT='巡检员值班状态变更日志';
```

---

### Phase 2: 后端开发（2小时）

#### 2.1 实体类修改
**文件：** `Patrol.java`

```java
/**
 * 是否接收通知：1=接收（值班中），0=不接收（离岗）
 */
private Integer notificationEnabled;

/**
 * 最后一次值班状态变更时间
 */
private LocalDateTime lastStatusChangeTime;
```

#### 2.2 Controller接口
**文件：** `PatrolController.java`

**接口1：切换值班状态**
```java
@PostMapping("/toggleNotification")
public ResponseEntity<Result> toggleNotificationStatus(
    @RequestParam String openid, 
    @RequestParam Integer enabled)
```

**接口2：查询值班状态**
```java
@GetMapping("/getDutyStatus")
public ResponseEntity<Result> getDutyStatus(@RequestParam String openid)
```

#### 2.3 Service方法
**文件：** `PatrolService.java` / `PatrolServiceImpl.java`

**方法：查询值班中的巡检员**
```java
List<Patrol> getOnDutyPatrolsByCommunity(String community);
```

#### 2.4 推送逻辑改造
**文件：** `ParkingTimeoutMonitoringTask.java`

**改造点：**
- 超时提醒推送给值班中的巡检员
- 违规通知推送给值班中的巡检员

---

### Phase 3: 前端开发（2小时）

#### 3.1 违规查询页面
**文件：** `pagesE/violation/violation.vue`

**改造内容：**
1. 导航栏右侧添加值班开关
2. 页面加载时查询状态
3. 切换状态功能
4. 小程序内提醒（早上首次打开）

#### 3.2 自定义TabBar
**文件：** `components/custom-tabbar.vue`

**改造内容：**
1. 接收值班状态prop
2. 违规查询菜单显示徽标
3. TabBar上方显示状态横条
4. 点击横条切换状态

#### 3.3 新增违规页面（可选）
**文件：** `pagesE/violation/add-violation.vue`

**改造内容：**
- 同步显示值班状态（可选）

---

## 🎨 UI设计效果

### 导航栏开关
```
┌─────────────────────────────────┐
│ ← 违规车辆        [🟢 值班]    │ ← 值班中（绿色）
└─────────────────────────────────┘

┌─────────────────────────────────┐
│ ← 违规车辆        [⭕ 离岗]    │ ← 离岗（红色）
└─────────────────────────────────┘
```

### 状态横条
```
┌─────────────────────────────────┐
│ 🟢 值班中 - 正在接收提醒 点击切换 │ ← 绿色横条
└─────────────────────────────────┘

┌─────────────────────────────────┐
│ ⭕ 离岗 - 消息提醒已关闭 点击切换 │ ← 红色横条
└─────────────────────────────────┘
```

### TabBar徽标
```
┌─────────────────────────────────┐
│      ➕              🚗         │
│                    [🟢]         │ ← 绿色圆点（值班）
│   新增违规         违规查询     │
└─────────────────────────────────┘

┌─────────────────────────────────┐
│      ➕              🚗         │
│                    [⭕]         │ ← 红色圆点（离岗）
│   新增违规         违规查询     │
└─────────────────────────────────┘
```

### 小程序内提醒
```
早上8点后首次打开小程序（离岗状态）：

┌─────────────────────────────┐
│       值班提醒              │
│                             │
│ 早上好！您当前处于离岗状态， │
│ 是否开启值班模式以接收消息   │
│ 提醒？                      │
│                             │
│  [暂不上岗]   [立即上岗]    │
└─────────────────────────────┘
```

---

## 🔄 业务流程

### 流程1：切换值班状态
```
用户点击开关
    ↓
弹出二次确认框
    ↓
用户确认
    ↓
调用后端接口
    ↓
更新数据库
    ↓
返回成功
    ↓
更新前端状态
    ↓
显示Toast提示
    ↓
震动反馈
    ↓
TabBar徽标同步更新
```

### 流程2：推送消息过滤
```
预约超时触发
    ↓
查询预约信息
    ↓
根据预约类型确定推送对象
    ↓
查询当前车场值班中的巡检员
    ↓
过滤 notification_enabled = 1 的巡检员
    ↓
批量发送微信模板消息
    ↓
记录推送日志
```

### 流程3：小程序内提醒
```
打开违规查询页面
    ↓
查询当前值班状态
    ↓
判断：是否离岗？
    ↓ 是
判断：是否是新的一天？
    ↓ 是
判断：是否在8点后？
    ↓ 是
弹出提醒对话框
    ↓
用户点击"立即上岗"
    ↓
调用切换状态接口
    ↓
状态更新为值班中
```

---

## 📊 数据统计

### 统计1：值班时长
```sql
SELECT 
  patrol_name,
  DATE(change_time) as duty_date,
  SUM(CASE 
    WHEN new_status = 1 THEN TIMESTAMPDIFF(MINUTE, change_time, LEAD(change_time) OVER (PARTITION BY patrol_id ORDER BY change_time))
    ELSE 0 
  END) / 60.0 as duty_hours
FROM patrol_duty_log
WHERE patrol_id = ?
  AND change_time >= ?
  AND change_time <= ?
GROUP BY patrol_name, DATE(change_time);
```

### 统计2：消息推送统计
```sql
SELECT 
  recipient_type,
  message_type,
  COUNT(*) as total_count,
  SUM(CASE WHEN success = 1 THEN 1 ELSE 0 END) as success_count
FROM notification_log
WHERE openid = ?
  AND send_time >= ?
  AND send_time <= ?
GROUP BY recipient_type, message_type;
```

---

## ⚠️ 边界问题处理

### 问题1：全部离岗预警
**场景：** 当前车场所有巡检员都离岗

**解决方案：**
- 后台管理端显示红色警告
- 发送预警通知给管理员

### 问题2：推送失败记录
**解决方案：**
- 使用 `notification_log` 表记录所有推送结果
- 定期清理过期日志

### 问题3：新增巡检员默认状态
**解决方案：**
- 默认 `notification_enabled = 1`（值班中）
- 让巡检员主动关闭，而不是主动开启

### 问题4：长时间离岗检查
**解决方案：**
- 定时任务检查超过24小时离岗的巡检员
- 发送提醒给管理员

### 问题5：防止误触
**解决方案：**
- 切换状态前显示二次确认框
- 明确提示切换后的影响

---

## 🧪 测试用例

### 测试1：切换值班状态
- [ ] 值班 → 离岗切换成功
- [ ] 离岗 → 值班切换成功
- [ ] 切换后数据库状态正确
- [ ] 切换后前端状态同步
- [ ] TabBar徽标同步更新
- [ ] 状态横条同步更新

### 测试2：推送消息过滤
- [ ] 值班中的巡检员收到推送
- [ ] 离岗的巡检员不收到推送
- [ ] 多个巡检员状态不同时过滤正确
- [ ] 推送日志记录正确

### 测试3：小程序内提醒
- [ ] 离岗状态早上首次打开显示提醒
- [ ] 值班状态不显示提醒
- [ ] 同一天多次打开不重复提醒
- [ ] 点击"立即上岗"成功切换状态

### 测试4：边界情况
- [ ] 所有巡检员离岗时预警正常
- [ ] 网络异常时错误提示友好
- [ ] 并发切换状态不出错

---

## 📝 代码清单

### 后端文件
- [ ] `sql/patrol_duty_management.sql` - 数据库脚本
- [ ] `entity/Patrol.java` - 实体类修改
- [ ] `controller/PatrolController.java` - 接口添加
- [ ] `service/PatrolService.java` - Service接口
- [ ] `service/impl/PatrolServiceImpl.java` - Service实现
- [ ] `task/ParkingTimeoutMonitoringTask.java` - 推送逻辑改造
- [ ] `controller/ViolationsController.java` - 违规推送改造

### 前端文件
- [ ] `pagesE/violation/violation.vue` - 违规查询页面
- [ ] `components/custom-tabbar.vue` - TabBar组件
- [ ] `pagesE/violation/add-violation.vue` - 新增违规页面（可选）

---

## ⏱️ 预估工时

| 阶段 | 工作内容 | 预估时间 |
|-----|---------|---------|
| Phase 1 | 数据库改造 | 10分钟 |
| Phase 2 | 后端开发 | 2小时 |
| Phase 3 | 前端开发 | 2小时 |
| Phase 4 | 联调测试 | 1小时 |
| Phase 5 | 上线部署 | 30分钟 |
| **总计** | | **约6小时** |

---

## 🚀 部署步骤

1. **备份数据库**
   ```bash
   mysqldump -u root -p parking_db > backup_$(date +%Y%m%d).sql
   ```

2. **执行数据库脚本**
   ```bash
   mysql -u root -p parking_db < patrol_duty_management.sql
   ```

3. **部署后端代码**
   - 打包：`mvn clean package`
   - 上传jar包
   - 重启服务：`sh restart.sh`

4. **发布前端代码**
   - 构建：`npm run build:mp-weixin`
   - 上传小程序
   - 提交审核

5. **监控日志**
   - 观察接口调用情况
   - 检查推送是否正常过滤
   - 查看用户反馈

---

## 📞 联系方式

如有问题，请联系开发团队。

**最后更新：** 2025-12-04
**文档版本：** v1.0
