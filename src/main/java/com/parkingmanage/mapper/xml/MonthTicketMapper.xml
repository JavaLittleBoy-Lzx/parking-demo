<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.parkingmanage.mapper.MonthTicketMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.parkingmanage.entity.MonthTick">

    </resultMap>
    <select id="selectAll" resultType="com.parkingmanage.entity.MonthTick">
        SELECT * FROM month_tick where park_name = #{parkName}
    </select>


</mapper>
