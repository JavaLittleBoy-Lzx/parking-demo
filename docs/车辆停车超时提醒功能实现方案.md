# 车辆停车超时提醒功能实现方案

## 1. 需求分析

### 1.1 功能描述
- **核心需求**：不管是哪个角色预约了车辆，车辆进场后，根据车场设置的停车超时时间，在用户距离超时前15分钟通过公众号消息推送提醒用户
- **适用角色**：所有预约车辆的用户（业主、访客、临时车辆等）
- **提醒方式**：微信公众号订阅消息推送
- **提醒时机**：超时前15分钟

### 1.2 业务流程
1. 用户预约车辆进场
2. 车辆实际进场，系统记录进场时间
3. 系统根据车场超时时间设置计算预计离场时间
4. 定时任务检查所有在场车辆，筛选出即将超时的车辆（15分钟内）
5. 发送公众号消息提醒用户

## 2. 系统架构分析

### 2.1 现有相关模块
- **VehicleReservation**：车辆预约实体，记录预约和进场信息
- **SubscribeMessageController**：订阅消息推送控制器
- **定时任务基础**：已有@Scheduled注解的定时任务示例

### 2.2 需要新增的模块
1. **停车超时时间配置表**
2. **消息提醒记录表**
3. **超时提醒定时任务**
4. **微信公众号消息模板**

## 3. 数据库设计

### 3.1 停车超时时间配置表 (parking_timeout_config)
```sql
CREATE TABLE `parking_timeout_config` (
  `id` int(11) NOT NULL AUTO_INCREMENT COMMENT '主键ID',
  `yard_code` varchar(50) DEFAULT NULL COMMENT '车场编码',
  `yard_name` varchar(100) DEFAULT NULL COMMENT '车场名称',
  `vehicle_type` varchar(50) DEFAULT NULL COMMENT '车辆类型（临时、访客、业主等）',
  `timeout_minutes` int(11) DEFAULT NULL COMMENT '超时时间（分钟）',
  `is_active` tinyint(1) DEFAULT '1' COMMENT '是否启用：1-启用，0-禁用',
  `create_time` datetime DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `update_time` datetime DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  PRIMARY KEY (`id`),
  KEY `idx_yard_code` (`yard_code`),
  KEY `idx_vehicle_type` (`vehicle_type`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='停车超时时间配置表';
```

### 3.2 消息提醒记录表 (message_notification_log)
```sql
CREATE TABLE `message_notification_log` (
  `id` int(11) NOT NULL AUTO_INCREMENT COMMENT '主键ID',
  `reservation_id` int(11) DEFAULT NULL COMMENT '预约记录ID',
  `plate_number` varchar(20) DEFAULT NULL COMMENT '车牌号码',
  `openid` varchar(100) DEFAULT NULL COMMENT '微信OpenID',
  `message_type` varchar(50) DEFAULT NULL COMMENT '消息类型（timeout_warning）',
  `message_content` text COMMENT '消息内容',
  `send_time` datetime DEFAULT NULL COMMENT '发送时间',
  `send_status` tinyint(1) DEFAULT '0' COMMENT '发送状态：0-待发送，1-已发送，2-发送失败',
  `error_message` text COMMENT '错误信息',
  `create_time` datetime DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  PRIMARY KEY (`id`),
  KEY `idx_reservation_id` (`reservation_id`),
  KEY `idx_plate_number` (`plate_number`),
  KEY `idx_send_time` (`send_time`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='消息提醒记录表';
```

### 3.3 修改现有车辆预约表
需要在 `VehicleReservation` 表中添加字段：
```sql
ALTER TABLE `vehicle_reservation` 
ADD COLUMN `openid` varchar(100) DEFAULT NULL COMMENT '用户微信OpenID',
ADD COLUMN `expected_leave_time` datetime DEFAULT NULL COMMENT '预计离场时间',
ADD COLUMN `timeout_notified` tinyint(1) DEFAULT '0' COMMENT '是否已发送超时提醒：0-未发送，1-已发送';
```

## 4. 核心功能实现

### 4.1 实体类设计

#### 4.1.1 停车超时配置实体
```java
@Data
@ApiModel(value="ParkingTimeoutConfig", description="停车超时时间配置")
public class ParkingTimeoutConfig {
    @ApiModelProperty(value = "主键ID")
    @TableId(value = "id", type = IdType.AUTO)
    private Integer id;
    
    @ApiModelProperty(value = "车场编码")
    private String yardCode;
    
    @ApiModelProperty(value = "车场名称")
    private String yardName;
    
    @ApiModelProperty(value = "车辆类型")
    private String vehicleType;
    
    @ApiModelProperty(value = "超时时间（分钟）")
    private Integer timeoutMinutes;
    
    @ApiModelProperty(value = "是否启用")
    private Boolean isActive;
    
    @ApiModelProperty(value = "创建时间")
    @TableField(fill = FieldFill.INSERT)
    private Date createTime;
    
    @ApiModelProperty(value = "更新时间")
    @TableField(fill = FieldFill.INSERT_UPDATE)
    private Date updateTime;
}
```

#### 4.1.2 消息提醒记录实体
```java
@Data
@ApiModel(value="MessageNotificationLog", description="消息提醒记录")
public class MessageNotificationLog {
    @ApiModelProperty(value = "主键ID")
    @TableId(value = "id", type = IdType.AUTO)
    private Integer id;
    
    @ApiModelProperty(value = "预约记录ID")
    private Integer reservationId;
    
    @ApiModelProperty(value = "车牌号码")
    private String plateNumber;
    
    @ApiModelProperty(value = "微信OpenID")
    private String openid;
    
    @ApiModelProperty(value = "消息类型")
    private String messageType;
    
    @ApiModelProperty(value = "消息内容")
    private String messageContent;
    
    @ApiModelProperty(value = "发送时间")
    private Date sendTime;
    
    @ApiModelProperty(value = "发送状态")
    private Integer sendStatus;
    
    @ApiModelProperty(value = "错误信息")
    private String errorMessage;
    
    @ApiModelProperty(value = "创建时间")
    @TableField(fill = FieldFill.INSERT)
    private Date createTime;
}
```

### 4.2 服务层实现

#### 4.2.1 停车超时提醒服务
```java
@Service
@Slf4j
public class ParkingTimeoutNotificationService {
    
    @Autowired
    private VehicleReservationService vehicleReservationService;
    
    @Autowired
    private ParkingTimeoutConfigService parkingTimeoutConfigService;
    
    @Autowired
    private MessageNotificationLogService messageNotificationLogService;
    
    @Autowired
    private WechatMessageService wechatMessageService;
    
    /**
     * 检查并发送超时提醒
     */
    public void checkAndSendTimeoutNotifications() {
        log.info("开始检查车辆停车超时提醒");
        
        // 1. 获取所有在场的车辆（已进场但未离场）
        List<VehicleReservation> inParkingVehicles = getInParkingVehicles();
        
        // 2. 筛选出即将超时的车辆（15分钟内）
        List<VehicleReservation> timeoutWarningVehicles = filterTimeoutWarningVehicles(inParkingVehicles);
        
        // 3. 发送提醒消息
        for (VehicleReservation vehicle : timeoutWarningVehicles) {
            try {
                sendTimeoutWarningMessage(vehicle);
            } catch (Exception e) {
                log.error("发送超时提醒失败，车牌号：{}", vehicle.getPlateNumber(), e);
            }
        }
        
        log.info("完成检查车辆停车超时提醒，共处理{}辆车", timeoutWarningVehicles.size());
    }
    
    /**
     * 获取所有在场的车辆
     */
    private List<VehicleReservation> getInParkingVehicles() {
        return vehicleReservationService.lambdaQuery()
            .isNotNull(VehicleReservation::getEnterTime)  // 已进场
            .isNull(VehicleReservation::getLeaveTime)     // 未离场
            .eq(VehicleReservation::getReserveFlag, 1)    // 已放行
            .eq(VehicleReservation::getTimeoutNotified, 0) // 未发送过提醒
            .list();
    }
    
    /**
     * 筛选即将超时的车辆（15分钟内）
     */
    private List<VehicleReservation> filterTimeoutWarningVehicles(List<VehicleReservation> vehicles) {
        List<VehicleReservation> result = new ArrayList<>();
        Date now = new Date();
        
        for (VehicleReservation vehicle : vehicles) {
            // 获取该车场和车辆类型的超时时间配置
            ParkingTimeoutConfig config = parkingTimeoutConfigService.getByYardCodeAndVehicleType(
                vehicle.getYardCode(), vehicle.getVehicleClassification());
            
            if (config == null || !config.getIsActive()) {
                continue;
            }
            
            // 计算预计离场时间
            Date enterTime = parseEnterTime(vehicle.getEnterTime());
            if (enterTime == null) {
                continue;
            }
            
            Calendar calendar = Calendar.getInstance();
            calendar.setTime(enterTime);
            calendar.add(Calendar.MINUTE, config.getTimeoutMinutes());
            Date expectedLeaveTime = calendar.getTime();
            
            // 计算距离超时的时间
            long timeDiff = expectedLeaveTime.getTime() - now.getTime();
            long minutesDiff = timeDiff / (60 * 1000);
            
            // 如果在15分钟内即将超时，加入提醒列表
            if (minutesDiff > 0 && minutesDiff <= 15) {
                vehicle.setExpectedLeaveTime(expectedLeaveTime);
                result.add(vehicle);
            }
        }
        
        return result;
    }
    
    /**
     * 发送超时提醒消息
     */
    private void sendTimeoutWarningMessage(VehicleReservation vehicle) {
        if (StringUtils.isEmpty(vehicle.getOpenid())) {
            log.warn("车牌号{}缺少OpenID，无法发送提醒", vehicle.getPlateNumber());
            return;
        }
        
        try {
            // 构造消息内容
            String messageContent = buildTimeoutWarningMessage(vehicle);
            
            // 发送微信消息
            boolean success = wechatMessageService.sendTimeoutWarningMessage(
                vehicle.getOpenid(), 
                vehicle.getPlateNumber(),
                vehicle.getYardName(),
                messageContent
            );
            
            // 记录发送日志
            MessageNotificationLog log = new MessageNotificationLog();
            log.setReservationId(vehicle.getId());
            log.setPlateNumber(vehicle.getPlateNumber());
            log.setOpenid(vehicle.getOpenid());
            log.setMessageType("timeout_warning");
            log.setMessageContent(messageContent);
            log.setSendTime(new Date());
            log.setSendStatus(success ? 1 : 2);
            messageNotificationLogService.save(log);
            
            // 更新车辆提醒状态
            if (success) {
                vehicle.setTimeoutNotified(1);
                vehicleReservationService.updateById(vehicle);
            }
            
        } catch (Exception e) {
            log.error("发送超时提醒失败", e);
            
            // 记录失败日志
            MessageNotificationLog errorLog = new MessageNotificationLog();
            errorLog.setReservationId(vehicle.getId());
            errorLog.setPlateNumber(vehicle.getPlateNumber());
            errorLog.setOpenid(vehicle.getOpenid());
            errorLog.setMessageType("timeout_warning");
            errorLog.setSendTime(new Date());
            errorLog.setSendStatus(2);
            errorLog.setErrorMessage(e.getMessage());
            messageNotificationLogService.save(errorLog);
        }
    }
    
    /**
     * 构造超时提醒消息内容
     */
    private String buildTimeoutWarningMessage(VehicleReservation vehicle) {
        SimpleDateFormat sdf = new SimpleDateFormat("MM月dd日 HH:mm");
        return String.format(
            "🚗 停车超时提醒\n\n" +
            "车牌号：%s\n" +
            "停车场：%s\n" +
            "预计离场时间：%s\n\n" +
            "您的车辆将在15分钟内超时，请及时离场！",
            vehicle.getPlateNumber(),
            vehicle.getYardName(),
            sdf.format(vehicle.getExpectedLeaveTime())
        );
    }
    
    /**
     * 解析进场时间字符串
     */
    private Date parseEnterTime(String enterTimeStr) {
        if (StringUtils.isEmpty(enterTimeStr)) {
            return null;
        }
        
        try {
            SimpleDateFormat sdf = new SimpleDateFormat("yyyy-MM-dd HH:mm:ss");
            return sdf.parse(enterTimeStr);
        } catch (Exception e) {
            log.error("解析进场时间失败：{}", enterTimeStr, e);
            return null;
        }
    }
}
```

#### 4.2.2 微信消息服务
```java
@Service
@Slf4j
public class WechatMessageService {
    
    @Value("${wechat.appid}")
    private String appId;
    
    @Value("${wechat.secret}")
    private String appSecret;
    
    @Value("${wechat.timeout.template.id}")
    private String timeoutTemplateId;
    
    private String accessToken;
    private Date tokenExpireTime;
    
    /**
     * 发送超时提醒消息
     */
    public boolean sendTimeoutWarningMessage(String openid, String plateNumber, 
                                           String yardName, String messageContent) {
        try {
            // 获取AccessToken
            String token = getAccessToken();
            if (StringUtils.isEmpty(token)) {
                log.error("获取AccessToken失败");
                return false;
            }
            
            // 构造消息数据
            Map<String, Object> messageData = buildMessageData(plateNumber, yardName, messageContent);
            
            // 构造请求体
            Map<String, Object> requestBody = new HashMap<>();
            requestBody.put("touser", openid);
            requestBody.put("template_id", timeoutTemplateId);
            requestBody.put("data", messageData);
            
            // 发送消息
            String url = "https://api.weixin.qq.com/cgi-bin/message/template/send?access_token=" + token;
            String response = HttpClientUtil.doPostJson(url, JSON.toJSONString(requestBody));
            
            log.info("微信消息发送响应：{}", response);
            
            // 解析响应
            JSONObject jsonResponse = JSON.parseObject(response);
            int errcode = jsonResponse.getIntValue("errcode");
            
            return errcode == 0;
            
        } catch (Exception e) {
            log.error("发送微信消息失败", e);
            return false;
        }
    }
    
    /**
     * 获取AccessToken
     */
    private String getAccessToken() {
        // 如果token未过期，直接返回
        if (accessToken != null && tokenExpireTime != null && 
            new Date().before(tokenExpireTime)) {
            return accessToken;
        }
        
        try {
            Map<String, String> params = new HashMap<>();
            params.put("grant_type", "client_credential");
            params.put("appid", appId);
            params.put("secret", appSecret);
            
            String response = HttpClientUtil.doPost("https://api.weixin.qq.com/cgi-bin/token", params);
            JSONObject jsonResponse = JSON.parseObject(response);
            
            if (jsonResponse.containsKey("access_token")) {
                accessToken = jsonResponse.getString("access_token");
                int expiresIn = jsonResponse.getIntValue("expires_in");
                
                // 设置过期时间（提前5分钟过期）
                Calendar calendar = Calendar.getInstance();
                calendar.add(Calendar.SECOND, expiresIn - 300);
                tokenExpireTime = calendar.getTime();
                
                return accessToken;
            } else {
                log.error("获取AccessToken失败：{}", response);
                return null;
            }
            
        } catch (Exception e) {
            log.error("获取AccessToken异常", e);
            return null;
        }
    }
    
    /**
     * 构造消息数据
     */
    private Map<String, Object> buildMessageData(String plateNumber, String yardName, String messageContent) {
        Map<String, Object> data = new HashMap<>();
        
        // 根据微信模板消息格式构造数据
        data.put("first", createDataItem("🚗 停车超时提醒", "#FF0000"));
        data.put("keyword1", createDataItem(plateNumber, "#173177"));
        data.put("keyword2", createDataItem(yardName, "#173177"));
        data.put("keyword3", createDataItem("即将超时", "#FF0000"));
        data.put("remark", createDataItem("请及时离场，避免产生额外费用！", "#173177"));
        
        return data;
    }
    
    /**
     * 创建数据项
     */
    private Map<String, String> createDataItem(String value, String color) {
        Map<String, String> item = new HashMap<>();
        item.put("value", value);
        item.put("color", color);
        return item;
    }
}
```

### 4.3 定时任务实现

#### 4.3.1 修改SubscribeMessageController
```java
@RestController
@RequestMapping(value = "/parking", method = {RequestMethod.GET, RequestMethod.POST})
public class SubscribeMessageController {
    
    @Autowired
    private ParkingTimeoutNotificationService parkingTimeoutNotificationService;
    
    // 原有代码...
    
    /**
     * 停车超时提醒定时任务
     * 每5分钟执行一次
     */
    @Scheduled(fixedRate = 300000) // 5分钟
    public void checkParkingTimeoutNotifications() {
        log.info("执行停车超时提醒定时任务");
        try {
            parkingTimeoutNotificationService.checkAndSendTimeoutNotifications();
        } catch (Exception e) {
            log.error("停车超时提醒定时任务执行失败", e);
        }
    }
}
```

## 5. 配置和部署

### 5.1 应用配置
在 `application.yml` 中添加微信相关配置：
```yaml
wechat:
  appid: your_wechat_appid
  secret: your_wechat_secret
  timeout:
    template:
      id: your_template_id  # 微信模板消息ID
```

### 5.2 微信公众号配置
1. 在微信公众平台创建模板消息
2. 模板内容格式：
```
{{first.DATA}}
车牌号：{{keyword1.DATA}}
停车场：{{keyword2.DATA}}
状态：{{keyword3.DATA}}
{{remark.DATA}}
```

### 5.3 数据库初始化
1. 创建相关表结构
2. 初始化停车超时时间配置数据：
```sql
INSERT INTO parking_timeout_config (yard_code, yard_name, vehicle_type, timeout_minutes, is_active) VALUES
('YARD001', '停车场A', '临时', 120, 1),
('YARD001', '停车场A', '访客', 180, 1),
('YARD001', '停车场A', '业主', 720, 1);
```

## 6. 测试方案

### 6.1 单元测试
1. 测试超时时间计算逻辑
2. 测试消息发送功能
3. 测试定时任务执行

### 6.2 集成测试
1. 模拟车辆进场场景
2. 验证定时任务正常运行
3. 验证消息推送到达

### 6.3 压力测试
1. 大量车辆同时进场的场景
2. 消息发送并发性能测试

## 7. 监控和运维

### 7.1 日志监控
- 定时任务执行日志
- 消息发送成功/失败日志
- 性能指标日志

### 7.2 异常处理
- 微信接口调用失败重试机制
- 数据库异常处理
- 定时任务异常恢复

### 7.3 性能优化
- 数据库查询优化
- 消息发送批量处理
- 缓存机制优化

## 8. 扩展功能

### 8.1 多级提醒
- 超时前30分钟提醒
- 超时前15分钟提醒
- 超时前5分钟提醒
- 超时后立即提醒

### 8.2 个性化配置
- 用户自定义提醒时间
- 不同车辆类型不同提醒策略
- 提醒方式选择（短信、微信、邮件）

### 8.3 数据分析
- 超时率统计
- 提醒效果分析
- 用户行为分析

## 9. 总结

该方案基于现有的系统架构，通过以下几个关键点实现停车超时提醒功能：

1. **数据模型**：扩展车辆预约表，新增超时配置表和消息日志表
2. **业务逻辑**：定时检查在场车辆，计算超时时间，发送提醒消息
3. **消息推送**：集成微信公众号API，发送模板消息
4. **定时任务**：每5分钟执行一次检查，确保及时提醒
5. **监控运维**：完善的日志记录和异常处理机制

该方案具有良好的扩展性和可维护性，能够满足当前需求，并为后续功能扩展提供基础。 