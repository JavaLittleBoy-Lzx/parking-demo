# 快速开始 - 测试访客预约接口

## 🚀 5分钟快速上手

### Step 1: 创建数据库表（1分钟）

```bash
# 在项目根目录执行
cd D:\PakingDemo\parking-demo

# 连接数据库并执行SQL
mysql -u root -p parking_management < docs/test_visitor_reservation.sql
```

**验证**：
```sql
mysql> USE parking_management;
mysql> SHOW TABLES LIKE 'test_visitor_reservation';
mysql> SELECT COUNT(*) FROM test_visitor_reservation;
# 应该看到 11 条初始数据
```

---

### Step 2: 启动应用（1分钟）

```bash
# 方式1：使用IDE
# 运行 Application.java 主类

# 方式2：使用Maven
mvn spring-boot:run
```

**验证**：
```bash
# 访问 Swagger 文档
http://www.xuerparking.cn:8080/swagger-ui.html

# 或直接测试接口
curl "http://www.xuerparking.cn:8080/parking/nefuData/page"
```

---

### Step 3: 测试接口（3分钟）

#### 3.1 测试分页查询

```bash
# 查询第1页（每页10条）
curl "http://www.xuerparking.cn:8080/parking/nefuData/page?pageNum=1&pageSize=10"
```

**预期响应**：
```json
{
  "code": "0",
  "msg": "success",
  "data": {
    "records": [...],  // 10条记录
    "total": 11,
    "current": 1,
    "pages": 2
  }
}
```

#### 3.2 测试时间范围查询

```bash
# 查询今天的预约
curl "http://www.xuerparking.cn:8080/parking/nefuData/page?startTime=2024-01-01%2000:00:00&endTime=2024-01-01%2023:59:59"
```

#### 3.3 生成更多测试数据

```bash
# 生成20条测试数据
curl -X POST "http://www.xuerparking.cn:8080/parking/nefuData/generateTestData?count=20"
```

**预期响应**：
```json
{
  "code": "0",
  "msg": "success",
  "data": "成功生成 20 条测试数据"
}
```

#### 3.4 查看统计信息

```bash
curl "http://www.xuerparking.cn:8080/parking-nefuData/statistics"
```

---

## 🔗 集成到自动开通服务

### 修改配置文件

编辑 `application.yml`（另一个项目）：

```yaml
visitor:
  api:
    url: http://www.xuerparking.cn:8080  # ← 改为测试接口地址
    page-size: 1000
  auto:
    enabled: true
```

### 启动自动开通服务

```bash
# 启动另一个项目（nefu-edu-datav/boot）
# 观察日志输出
```

**预期日志**：
```
📤 [外部接口请求] URL: http://www.xuerparking.cn:8080/parking/nefuData/page
📥 [外部接口响应] 成功
📄 [第1页] 获取到 31 条预约记录
🆕 [新增] 预约ID: R2024010101, 访客: 张三
```

---

## 📝 完整测试流程

### 1. 清空旧数据，重新开始

```bash
# 清空所有测试数据
curl -X DELETE "http://www.xuerparking.cn:8080/parking/nefuData/clearTestData"

# 重新生成10条
curl -X POST "http://www.xuerparking.cn:8080/parking/nefuData/generateTestData?count=10"
```

### 2. 验证自动开通服务

```bash
# 1. 启动测试接口（本项目）
# 2. 启动自动开通服务（另一个项目）
# 3. 观察日志，确认数据正常同步
```

### 3. 测试增量同步

```bash
# 1. 等待自动开通服务完成首次同步

# 2. 添加5条新数据
curl -X POST "http://www.xuerparking.cn:8080/parking/nefuData/generateTestData?count=5"

# 3. 等待2秒（自动开通服务的轮询间隔）

# 4. 观察日志，应该只处理新增的5条
```

---

## 🎯 常用命令速查

```bash
# 【查询】
# 查询所有预约（每页1000条）
curl "http://www.xuerparking.cn:8080/parking/nefuData/page?pageSize=1000"

# 查询今天的预约
curl "http://www.xuerparking.cn:8080/parking/nefuData/page?startTime=2024-01-01%2000:00:00&endTime=2024-01-01%2023:59:59"

# 【生成数据】
# 生成10条测试数据
curl -X POST "http://www.xuerparking.cn:8080/parking/nefuData/generateTestData?count=10"

# 【清空数据】
curl -X DELETE "http://www.xuerparking.cn:8080/parking/nefuData/clearTestData"

# 【统计】
curl "http://www.xuerparking.cn:8080/parking/nefuData/statistics"

# 【添加单条】
curl -X POST "http://www.xuerparking.cn:8080/parking/nefuData/add" \
  -H "Content-Type: application/json" \
  -d '{"reservationId":"R999","visitorName":"测试","carNumber":"京A99999","vipTypeName":"临时访客"}'
```

---

## ✅ 验证清单

- [ ] 数据库表已创建
- [ ] 初始数据已插入（11条）
- [ ] 应用启动成功
- [ ] 分页查询接口正常
- [ ] 时间范围查询正常
- [ ] 生成测试数据成功
- [ ] 统计接口正常
- [ ] 集成到自动开通服务成功

---

## 🐛 常见问题

### Q1: 接口返回404

**A**: 检查应用是否启动，端口是否正确（默认8080）

### Q2: 查询返回空数据

**A**: 
```bash
# 检查数据库数据
mysql> SELECT COUNT(*) FROM test_visitor_reservation;

# 如果为0，重新执行SQL
mysql -u root -p parking_management < docs/test_visitor_reservation.sql
```

### Q3: 自动开通服务连接失败

**A**: 
```yaml
# 确认配置正确
visitor:
  api:
    url: http://www.xuerparking.cn:8080  # 确保地址正确
```

---

## 📚 更多文档

- [详细接口文档](测试接口使用说明.md)
- [数据库表结构](test_visitor_reservation.sql)

---

## 🎉 完成！

现在你已经成功搭建了测试接口，可以：

- ✅ 模拟外部访客预约系统
- ✅ 测试VIP自动开通功能
- ✅ 验证增量同步逻辑
- ✅ 调试和优化系统

**Happy Testing!** 🚀

